apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-portal.fullname" . }}
  labels:
    {{- include "cf-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-portal.selectorLabels" . | nindent 4 }}
