apiVersion: v1
kind: Service
metadata:
  name: employee-service
  labels:
    app: employee-service
spec:
  selector:
    app: employee-service
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
