apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-connector-service
  labels:
    app: ai-connector-service
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-connector-service
  template:
    metadata:
      labels:
        app: ai-connector-service
        component: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8001"
    spec:
      serviceAccountName: careerforge-backend
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: ai-connector-service
          image: gitea.local/careerforge/backend:latest
          command: ["uvicorn", "services.ai_connector.main:app", "--host", "0.0.0.0", "--port", "8001", "--workers", "2"]
          ports:
            - containerPort: 8001
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: careerforge-db
                  key: url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: careerforge-redis
                  key: url
          envFrom:
            - configMapRef:
                name: careerforge-common
          volumeMounts:
            - name: vault-secrets
              mountPath: /vault/secrets
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: vault-secrets
          projected:
            sources:
              - serviceAccountToken:
                  path: vault-token
                  expirationSeconds: 7200
                  audience: vault
